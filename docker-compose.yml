services:
  household-hub:
    build: .
    container_name: household-hub
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config.yml:/data/config.yml:ro
    environment:
      - TZ=America/Chicago
